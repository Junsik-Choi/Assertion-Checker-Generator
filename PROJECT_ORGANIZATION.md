# Project Organization Summary

프로젝트 구조 정리가 완료되었습니다!

## 📁 새로운 폴더 구조

```
Assertion-Checker-Generator/
├── scripts/                  # 메인 소스 코드
│   ├── cli_tui.py           # 메인 CLI 인터페이스
│   ├── fill_define.py       # Excel 정의 시트 채우기
│   ├── assertion_gen.py     # 어설션 생성 로직
│   ├── rtl_parser.py        # RTL 파서
│   ├── assertions/          # 어설션 플러그인
│   └── help_config.json     # 도움말 설정
│
├── tests/                   # 테스트 파일 (29개)
│   ├── test_*.py            # 기능 테스트 스크립트
│   └── README.md            # 테스트 가이드
│
├── dev/                     # 개발/디버그 파일 (18개)
│   ├── debug_*.py           # 디버그 스크립트
│   ├── analyze_*.py         # 분석 유틸리티
│   ├── check_*.py           # 검증 스크립트
│   ├── run_*.py             # 실행 스크립트
│   ├── *.log                # 로그 파일
│   └── README.md            # 개발 가이드
│
├── docs/                    # 문서 & 참고 자료 (25개)
│   ├── *.md                 # 마크다운 문서
│   ├── *.txt                # 텍스트 문서
│   └── README.md            # 문서 가이드
│
├── EDA/                     # EDA 관련 샘플 데이터
├── Data/                    # 데이터 파일
├── out/                     # 출력 디렉토리 (자동 생성)
├── logs/                    # 로그 디렉토리
├── trash/                   # 휴지통 (불필요한 파일)
│
├── README.md                # 프로젝트 메인 문서
├── LICENSE                  # 라이센스
└── .git/                    # Git 저장소

```

## 📊 정리 결과

| 폴더 | 파일 개수 | 설명 |
|------|---------|------|
| tests | 29개 | 테스트 스크립트 |
| dev | 18개 | 개발/디버그 유틸리티 |
| docs | 25개 | 문서 & 참고 자료 |
| **총** | **72개** | 정리된 파일 |

## 🧹 삭제/이동된 항목

✅ 모든 `test_*.py` 파일 → `tests/` 폴더로 이동  
✅ 모든 `debug_*.py`, `analyze_*.py` 등 → `dev/` 폴더로 이동  
✅ 모든 마크다운 & 텍스트 문서 → `docs/` 폴더로 이동  
✅ 로그 파일 → `dev/` 폴더로 이동  
✅ `define.xlsx` (테스트용) → `trash/` 폴더로 이동  

**루트 디렉토리**: 이제 깔끔하고 정돈되었습니다! (필수 파일만 남음)

## 📌 주요 개선사항

1. **가독성 향상**: 루트 디렉토리가 훨씬 깨끗해졌습니다
2. **논리적 구성**: 용도별로 파일이 정리되었습니다
3. **유지보수 용이**: 필요한 파일을 쉽게 찾을 수 있습니다
4. **개발 효율성**: 테스트와 개발 파일이 분리되었습니다

## 🚀 사용 가이드

### 테스트 실행
```bash
cd tests
python test_wizard_flow.py
```

### 개발 스크립트 실행
```bash
cd dev
python debug_hierarchy.py
```

### 문서 참고
```bash
cd docs
# 마크다운 파일들 확인
```

---

**정리 완료 날짜**: 2025-11-10  
**정리 전 파일**: 루트에 100개 이상의 잡다한 파일  
**정리 후**: 깔끔하게 3개 폴더로 분류 ✨
